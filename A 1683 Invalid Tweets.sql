SELECT tweet_id 
FROM Tweets 
WHERE LENGTH(content)>15;

# The first statement select the tweet_id to display the id of the person content greater than 15

# The Length() function provide the number of character and it is check whether is within 15 character